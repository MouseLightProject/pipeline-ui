import * as React from "react";

import {BodyContainerWithQuery} from "./GraphQLComponents";

export class Layout extends React.Component<any, any> {
    render() {
        return (
            <BodyContainerWithQuery/>
        )
    }
}
